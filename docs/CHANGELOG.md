# Changelog

## Purpose
Record meaningful changes to the Mattress 3D Render project.

## Format

### YYYY-MM-DD — Change title
- **Changed:** 
- **Reason:** 
- **Files/areas:** 
- **Impact:** 
- **Validation:** 

---

### 2026-08-31 — Duro's green band is real, and Riva's R stack is named
- **Changed:**
  - **Duro gains a fifth band, and its top grade is the one without it.** `Float Sense Foam` sits between AeroFlex and OrthoBond in **Classic 5″ and Premium 6″**; **Luxury 6″ omits it** and renders exactly the four-band stack it renders today. Duro is now the one product whose top grade carries *fewer* bands than the grades below it, which is what all three renders in `Layers/` show.
  - **Riva's R-grade stack is named:** Bio Weave / SetaComforto™ / LussoMorbido™ / NubeSoave™ / Struttura Forte™ / FondoCore Italia™ / Quilted Foam. R3000 shows all six, R2000 and R1000 show it less the bands they drop. These are the only names in either brand carrying a trademark symbol, which is the owner's decision.
- **This overrides a Confirmed catalog entry, and why it was wrong is the point.** Duro's green slab has been recorded since the beginning as a render artefact that "does not exist in the real product and must never be rendered" - the catalog used it as its own worked example of an image losing to the table. That reading assumed every Duro grade shares one stack, so two renders with different band counts had to mean one was faulty. **They were never in conflict; they are different grades.** The product owner confirms the band is real and is Float Sense Foam, the same material Maxa and Magic carry, and it takes the same albedo here - the green in the render is `#6BC163`, which is exactly Maxa's Float Sense, so the colour corroborates the identification independently.
- **A lesson recorded in the catalog rather than just fixed:** two renders showing different band counts is not evidence that one of them is faulty. Read that way once, it cost this project a real layer for weeks. Where renders disagree, per-grade composition is the first explanation to test, not the last.
- **Numbering:** OrthoBond moves from Layer 3 to Layer 4, because numbering follows position in the full stack. **Nothing on screen changes** - Duro's bands carry real names, so no label ever showed a number.
- **The foam split still holds.** "OrthoBond is 100% of Duro's support foam" is unaffected: Float Sense is comfort foam, not support foam, so it does not divide the share that split governs.
- **The RIVA 3000 reference sheet is followed on six lines and not on two.** It names layer 1 "UltraLuxe Adaptive Knit Fabric" and the base "Soft Foam with Quilting"; layer 1 stays **Bio Weave** on the owner's explicit instruction and the base stays **Quilted Foam** under the everywhere rule. Recorded in the catalog so the sheet is not applied wholesale later.
- **Reason:** Product owner, 2026-08-31. Three ambiguities were put to them first rather than guessed: whether the green band overrides the never-render rule (it does, and it is Float Sense Foam), whether "Riva Luxury" meant a new grade (it meant R3000), and the spelling and trademark style of the Italian names (SetaComforto, with ™).
- **Files/areas:** `app/src/data/layers/vedasleepLayers.js`, `app/src/data/products.js`, `app/src/data/layers/foamicoLayers.js`, `docs/PRODUCT_CATALOG.md`.
- **Validation:** `npm run build` and `oxlint` clean. Duro resolves to five bands at Classic and Premium and four at Luxury, with the Luxury set identical to what shipped before; Riva resolves to all six names plus the base. Ratios are normalised at build time, so inserting the band re-divides the stack and every other Duro band keeps the proportion against its neighbours that it already had.

---

### 2026-08-31 — Cosmic Foam, and the perforation gap is accepted
- **Changed:** **Sova Natural's layer 3 is `Cosmic Foam`, not `Cosmic Byte Foam`.** It was supplied as "Cosmic Byte Foam" and flagged in the entry below rather than reconciled, because the same purple comfort foam is `Cosmic Foam` in Sova's standard grade and a silent fix to a material name is exactly the kind of invention this project does not allow. The product owner confirmed it is Cosmic Foam. Sova Natural now reads AirKnit Fabric / AeroFlex Foam / Cosmic Foam / Pincore Latex / OrthoBond Foam / Quilted Foam, and the same name means the same material at both grades.
- **Closed, not outstanding:** the pin-perforations of `Pincore Latex` and `7 Zone Latex` are **deliberately not rendered**. The renderer's surface vocabulary has no perforated option; the product owner accepted the gap rather than have one added. Recorded in `docs/PRODUCT_CATALOG.md` as a decision so it is not picked up later as a bug - those latex bands draw plain on purpose.
- **Reason:** Product owner, 2026-08-31, answering both flags raised with the previous change.
- **Files/areas:** `app/src/data/layers/foamicoLayers.js`, `docs/PRODUCT_CATALOG.md`.
- **Note on the record:** "Cosmic Byte Foam" survives in the two changelog entries below as what was superseded. Those are history and stay as written; the catalog is the source of truth and says Cosmic Foam.
- **Validation:** `npm run build` clean, and Sova Natural confirmed to resolve with the corrected name.

---

### 2026-08-31 — Leader lines, and the Natural layers get their names
- **Changed:**
  - **Every layer label is joined to its band by a leader line.** The printed-callout shape: a diagonal run from the band's own projected corner to an elbow, then a short horizontal into the pill. The bend is what makes it read as pointing at something rather than as a stray diagonal.
  - **The pills line up in one column** where there is room for one. This is the part that makes the feature work: parked individually beside their own band each pill sits a few pixels from its anchor and the leader is a stub, reading as a tick on the mattress rather than as a line joining two things. Drawn and looked at before shipping - the stub version was the first implementation and was visibly wrong. The column is used only when it clears the widest anchor; a narrow viewport falls back to a pill beside its own band.
  - **The Natural layers are named** on all three products: Sova Natural is AirKnit Fabric / AeroFlex Foam / Cosmic Byte Foam / Pincore Latex / OrthoBond Foam / Quilted Foam; Ultima Natural is AirKnit Fabric / AeroFlex Foam / Pincore Latex / Cloud Sense Foam / Zero G Latex / Quilted Foam; Riva Natural is Bio Weave / AeroFlex Foam / 7 Zone Latex / Quilted Foam. No `nameTbd` is left anywhere in a Natural stack.
- **The names corroborate the stacks, which is worth recording.** They arrived after the stacks had been read off the renders, and each list came out **exactly as long as the band count read from its own render**. Every material that also appears in the product's standard grade kept the same name there - Cloud Sense and Zero G on Ultima, OrthoBond on Sova - and `Pincore Latex` names the pin-perforations the renders show. Two independent sources agreeing is the strongest confirmation this project has for any Natural grade.
- **Flagged, not reconciled:** `Cosmic Byte Foam` is Sova Natural's layer 3, and the same purple comfort foam is `Cosmic Foam` in Sova's standard grade. Either a second material or a slip for the established name. Recorded exactly as given and flagged in the catalog; this needs the product owner rather than a quiet fix.
- **Reason:** Product owner, 2026-08-31, with a reference image of an exploded socket callout: lines connecting the foam layers to the mattress, plus the Natural layer names.
- **Files/areas:** `app/src/components/MattressViewer.jsx`, `app/src/index.css`, `app/src/data/layers/foamicoLayers.js`, `docs/INTERACTIONS.md`, `docs/PRODUCT_CATALOG.md`.
- **Also raised the label gap** from 14px to 24px. A leader needs a run to be read as a line, and the pill no longer needs to sit against the mattress now that something joins the two.
- **Validation:** `npm run build` and `oxlint` clean. All three Natural stacks confirmed to carry real names with no `nameTbd` remaining. The spacing pass and leader geometry were replicated exactly in a standalone script and rendered to an image, at seven and eight bands, which is how the stub problem was caught before shipping rather than after. **The live page is still not verified on screen** - there is no browser automation in this repo.

---

### 2026-08-31 — Natural gets its own stack, on all three products that offer it
- **Changed:** **Sova, Ultima and Riva Natural are built from `Layers/<PRODUCT> NATURAL.png`.** Natural is not its product's stack with bands removed - every Natural carries a perforated natural-latex slab no other grade has, in place of comfort foam - so there is no `omitLayers` list that could describe it.
  - **Ultima Natural, six bands:** cover → convoluted → latex → rebonded chip core → support foam → bonded base. The standard grade's blue and purple comfort foams are gone, not thinner.
  - **Sova Natural, six bands:** cover → convoluted → purple comfort foam → latex → rebonded chip core → bonded base. The latex sits under the purple here, where the standard grade has its blue band.
  - **Riva Natural, four bands:** cover → convoluted → latex → bonded base. The shortest stack of any FOAMICO grade, with the latex over half the mattress.
- **A variant may now carry its own `layers`**, which replace the product's for that grade; the omission and upholstery rules then apply to that set exactly as they would to the default one. One line in `variantLayers.js`, and the first thing in this project that needed it.
- **One new albedo across the three of them**, for the latex slab. Every other band reuses the colour its own product already established for that material, so switching to Natural never shifts a foam's colour - the bands that carry over look like themselves. The bonded base keeps each product's own base colour, because that band still wears the product's `bottom.png` and the base-colour rule in `docs/PRODUCT_CATALOG.md` is derived from that photograph.
- **The woven tag in the render set is unreliable, and this is worth recording.** Two of the three renders carry an "ULTIMA NATURAL" tag - including `SOVA NATURAL.png`, which shows a plainly different mattress from `ULTIMA NATURAL.png`. Put to the product owner, whose instruction was to go by the filename and disregard the tag. Recorded in the catalog so nobody "corrects" it back from the tags later.
- **Also:** the three Natural PNGs had gone missing from the working tree - tracked in git, deleted on disk - and were restored before any of this.
- **Reason:** Product owner, 2026-08-31: add the Natural images from the Layers folder to the designated space. Read as building the Natural grades' stacks, confirmed with the owner before starting; foam names to follow.
- **Files/areas:** `app/src/data/layers/foamicoLayers.js`, `app/src/data/foamicoProducts.js`, `app/src/lib/variantLayers.js`, `docs/PRODUCT_CATALOG.md`.
- **What is confirmed and what is not.** Band count, order, type and surface are read off the renders and are confirmed, as they are for every stack in this project. **Thicknesses are eyeballed placeholders** until the owner gives Natural's foam split - the renders draw the grades as a staircase with each slab's top face visible, so a scan down them measures top surfaces as much as thicknesses and cannot be trusted for proportion. **Names below the cover are `Layer N — TBD`**, awaiting the foam names. The cover takes the product's established layer-1 name and the bonded base is Quilted Foam per the everywhere rule.
- **Known fidelity gap:** the latex slab's pin-perforations are the most recognisable thing about it in the renders and are not represented - the renderer's surface vocabulary has no perforated option, so the band is drawn plain. Worth a `perforated` surface in `foamSurfaces.js` if Natural is to read properly.
- **Validation:** `npm run build` and `oxlint` clean. All three stacks resolve with ratios summing to 1.00, and all five standard FOAMICO stacks confirmed unchanged in band count.

---

### 2026-08-31 — Full-bleed stage, floating chrome
- **Changed:** **The canvas now fills the viewport and the chrome floats on it.** The title, variant pill and control row are positioned over the stage instead of taking layout space beside it. The stage box used to end above the controls and below the head, and those boundaries showed as a band of Key Black on FOAMICO and Stage Grey on VedaSleep - and cropped the product, so zooming in ran the mattress into a hard edge partway down the screen.
- **The camera is pulled back to match, and this is the part that is easy to get wrong.** The 32 degree fov is *vertical*: a taller canvas holds the same world height and proportionally less world *width*, about 50% less on a desktop window, which would have put a wide mattress straight into clipping at the sides - the opposite of the intent. The viewer measures the head and control row and pulls back by the ratio of the full viewport to what is left after them. That cancels exactly, so pixels-per-inch is unchanged and **the product renders at precisely the size and position it had inside the box**; only the background extends. Measured rather than hard-coded against a reference aspect, so it stays correct as the control row rewraps at the phone breakpoints.
- **The fit is applied once**, where the camera is positioned, so the view presets, the pinch clamps and the explode park all keep the logical units they were tuned in.
- **The floating bars do not swallow drags.** They are `pointer-events: none` and only the controls inside them take the pointer back; otherwise the bottom of the mattress would have become un-draggable.
- **Scoped to the mattress viewer** via `.mv-immersive`. Sofa cum Bed wears the same `.mv-head` / `.mv-controls` classes but is a photographic plate in normal flow and still wants them to occupy space - making the classes float globally would have broken that page.
- **Reason:** Product owner, 2026-08-31: make the CTAs floating; the black edges on FOAMICO and grey edges on VedaSleep should not be visible, and zooming into the product should reveal no edges. The full-bleed scope - floating the title as well as the buttons - was put to the owner and chosen, because floating the buttons alone leaves the top boundary intact.
- **Files/areas:** `app/src/components/MattressViewer.jsx`, `app/src/index.css`, `docs/3D_RENDER_GUIDELINES.md`.
- **Known consequence:** the product now passes behind the title and the control row when zoomed in, which is inherent to full-bleed and was accepted when the scope was chosen. No scrim was added - that would be a visible design addition nobody asked for. If legibility suffers over a white quilt, a soft gradient behind the head and controls is the fix.
- **Validation:** `npm run build` and `oxlint` clean. The camera fit was derived rather than tuned, and cancels algebraically: world height scales by `fit` while the canvas grows by the same factor. **Not verified on screen** - there is no browser automation in this repo.

---

### 2026-08-31 — The exploded stack backs off enough to see the whole product
- **Changed:** **`EXPLODE_SCALE` 0.62 -> 0.50.** The open stack was framed too close: on Ultima Luxury the bottom band came within a few pixels of the control row and the right edge of the mattress ran into its own labels. Every product now sits 1.01x-1.16x the framing that shipped before this week rather than 1.26x-1.44x.
- **Reason:** Product owner, 2026-08-31, on a screenshot of Ultima Luxury: the proportions are right, but the view is too close to see the product entire.
- **This closes the open clipping item** from the entry below, and it closes it with evidence rather than by guessing. Ultima at 1.33x was visibly the tightest a stack can get while still fitting, so anything at or under that fits; every product is now at most 1.16x, and Ultima itself is 1.07x - back to roughly the framing that shipped for weeks, but with bands two and a half times the air around them.
- **Nothing else moved.** `GAP_FRACTION` stays at 0.4, so the band-to-gap ratio the owner approved is untouched - scale divides bands and gaps alike. No layer data, no camera change (`EXPLODE_DIST` still 94), no code beyond the one constant.
- **Files/areas:** `app/src/components/MattressViewer.jsx`.
- **Validation:** `npm run build` clean. Re-measured across all eight products: Duro 1.03x, Maxa 1.01x, Magic 1.03x, Resto 1.07x, Sova 1.07x, Ultima 1.07x, Riva 1.13x, Luma 1.16x; band-to-gap still exactly 2.5:1 and still no overlapping pairs anywhere.

---

### 2026-08-31 — Bands dominate the gaps, and the stack sits larger
- **Changed:**
  - **The exploded gap is now a fraction of average band thickness** (`GAP_FRACTION`, 0.4) rather than a separation solved on its own. Solved on its own it came out as large as the slabs or larger, and a stack whose air dominates its foam reads as a row of uniform floating cards whatever the real proportions are. Every product now shows a band-to-gap ratio of exactly 2.5:1.
  - **`EXPLODE_SCALE` raised from 0.55 to 0.62**, which with the refit puts the open stack 1.26x-1.44x the size it was on screen. The camera is untouched: `EXPLODE_DIST` stays at 94.
  - **The layer data was not touched.** No `thicknessRatio` changed. Bands whose real specs are close still look close, because they are - this makes true proportions legible rather than inventing differences.
- **Deviation from the brief, and why it was necessary:** the brief said to keep `explodeDy = ((n-1)/2 - i) * gap` unchanged, which treats `gap` as centre-to-centre spacing, while its own `trueHeight = Hs + (n-1)*gap` treats `gap` as the clear air between bands. Those cannot both hold. Taken literally the layout breaks outright: at `GAP_FRACTION` 0.4 the centre-to-centre spacing is 0.88in on Resto against bands 1.41in to 4.40in thick, so **every band interpenetrates its neighbours** and the stack renders as a solid blob. The clear-air reading is the one the normalisation requires, so the bands are now laid out by walking the stack - each offset by half its own thickness, half the previous one's, and the gap. Verified: zero overlapping pairs across all eight products, and the band-to-gap ratio is exact for every pair rather than only on average.
  - Taken literally the brief also **shrank** the stack to 0.40x-0.57x of its previous on-screen size rather than enlarging it, because `Hs + (n-1)*gap` over-counts the exploded span by every band but one. With the layout corrected, the same normalisation delivers the roughly 20% the brief predicted, and 0.62 takes it to 1.26x-1.44x.
- **Knock-on fix:** `HOVER_LIFT` is 1.15in and used to lift a band into four or five inches of clear air. The air is now 0.88in on a seven-band product, so the same lift would push a hovered band up into the one above it. It is capped against the gap the stack actually has (`gap * 0.6`), so the lift still reads and cannot collide. Sizing change, but leaving it would have broken hover.
- **Reason:** Product owner, 2026-08-31: bands should dominate the gaps so proportions read, and the whole exploded stack should sit larger in the viewport.
- **Files/areas:** `app/src/lib/layerStack.js`, `app/src/components/MattressViewer.jsx`, `docs/3D_RENDER_GUIDELINES.md`. No layer data touched.
- **Measured, per product** (gap, thinnest..thickest band, size vs the framing that shipped before this work): Duro 1.32in, 2.64-3.96in, 1.27x. Maxa 1.10in, 1.08-5.02in, 1.26x. Magic 0.88in, 0.45-6.35in, 1.28x. Resto 0.88in, 1.41-4.40in, 1.33x. Sova same as Resto. Ultima 0.88in, 1.28-4.33in, 1.33x. Riva 1.13in, 1.80-5.60in, 1.40x. Luma 1.10in, 0.93-6.08in, **1.44x - the tallest, and the first place to look for clipping**.
- **Validation:** `npm run build` and `oxlint` clean. The exploded layout was replicated arithmetically for all eight products: no overlapping pairs, band-to-gap 2.5:1 everywhere. **Clipping is NOT verified** - it is a visual condition and there is no browser automation in this repo. Every product is larger than the framing that shipped before, so the fail condition the brief names is genuinely open. If anything clips, `EXPLODE_SCALE` is the single knob: 0.43 returns the worst case (Luma) to exactly the old framing, and about 0.50 caps every product at roughly 1.15x.

---

### 2026-08-31 — Ultima's Luxury is 7 inches
- **Changed:** **Ultima Luxury is 7″, not the 6.5″ this project has carried.** Corrected on the product owner's instruction against the variant menu on the live page. Ultima's Luxury is therefore the same height as Resto's and Sova's; the 6.5″ was the odd one out among the three seven-band FOAMICO products.
- **Reason:** Product owner, 2026-08-31: "luxury variant here is 7 inches, please change that."
- **Files/areas:** `app/src/data/foamicoProducts.js`, `docs/PRODUCT_CATALOG.md`.
- **Impact:** Luxury is Ultima's baseline grade — first in its `variants` list — so this is the height the product page opens on and renders at, not only a line in the menu. Band membership per grade is untouched: Luxury still carries all seven bands, and Premium and Classic still drop what they dropped. The other grades' heights are unchanged, which widens Ultima's ladder from 5″–6.5″ to 5″–7″; nothing in the layer stack is sensitive to that spread, because Ultima does not opt into `holdUpholstery`.
- **Validation:** `npm run build` clean. Grepped the tree for other copies of the 6.5″ figure: the only remaining ones are Resto's and Sova's Premium grades, which are genuinely 6.5″ and unrelated.

---

### 2026-08-31 — Broader bands, and labels that are spaced against each other
- **Changed:**
  - **Bands are built 2.2x thicker than the mattress they came out of.** A true 6"-10" split over six to eight layers leaves each band under an inch thick, and at the distance the exploded view is framed from that renders as a sheet of paper rather than a slab of foam - which is the one thing the layers view exists to show. The whole stack is squashed back to the product's real height while it is closed, so the exaggeration exists only in the open view.
  - **The closed mattress is untouched.** It is a separate mesh and keeps its declared thickness exactly. What is now deliberately wrong is the *exploded* stack: an exploded Classic and an exploded Luxury are no longer to scale against each other, and no dimension may be read off the open view. The product owner asked for broader layers and accepted this explicitly.
  - **The squash is undone across the whole explode, not at the end of the cross-fade.** The stack is therefore still near the product's real height for as long as the solid box is visible behind it, and only reaches full thickness long after the box has gone - so the exaggeration is never on screen next to the thing it exaggerates. Scaling every band and every rest offset by the same factor is a uniform scale of the stack about the origin, so the bands stay exactly contiguous at every point of the ramp and no seam opens up.
  - **Geometry is built oversize and scaled down, never the reverse.** The bands are at their native proportions exactly when they are being looked at, so the wall grain and the sculpted relief are never stretched in the open view. The squash lands on the closed stack, which is hidden behind the solid mattress.
  - **The frame is held constant rather than re-tuned per product.** Thicker bands would simply overflow, so the stack returns the ratio of what it would have spanned uninflated to what it spans now and the viewer multiplies its explode scale by it. Every product stays framed exactly as it was: bands come out 1.58x-1.87x thicker on screen and the air between them 0.72x-0.85x, with the total extent unchanged. Solved, not dialled in, so a product with a different band count needs no new number. The gap also carries the extra thickness on top of the separation it always had, or the slabs would close up on each other and stop reading as separate layers.
  - **Layer labels are spaced against each other, not only against their bands.** A label is anchored to its band's projected corner - that is what makes it point at its layer - but the anchor says nothing about the label beside it, and six to eight pills over the framed height sat closer together than a pill is tall. They are now pushed apart to a minimum pitch once every anchor for the frame is known, and the run is translated as a whole rather than clamped per label, so the list stays centred on the bands and no label crosses its neighbour.
  - **The spacing pass runs in screen order, not stack order.** `phi` clamps at -1.45, well below the horizon, so the camera can be orbited under the mattress - and from there the top band projects below the bottom one. Ordering by stack index would have dragged every label to the wrong end of the frame for the whole of that view. It also never asks for more pitch than the stage can give: on a short viewport the pills close up a little instead of the list detaching from the stack to span a height the mattress does not occupy.
- **Reason:** Product owner, 2026-08-31: "i want the layers of the foam to be broader. it is okay if it doesnt end up matching classic, premium and luxury sizes", and the layer labels are too congested.
- **Files/areas:** `app/src/lib/layerStack.js`, `app/src/components/MattressViewer.jsx`, `docs/3D_RENDER_GUIDELINES.md`, `docs/INTERACTIONS.md`, `docs/PRODUCT_CATALOG.md`.
- **Impact:** Exploded layer view only. The closed mattress, the grade ladder, layer order, band membership per grade, materials, colours and every layer name are unchanged. `docs/3D_RENDER_GUIDELINES.md`'s "exploded spacing without distorting the product" rule is amended rather than left stale: thickness may now be distorted, footprint and order still may not.
- **Numbers, per product** (band thickness on screen / air between bands on screen, total extent unchanged): Duro 1.81x / 0.82x, Maxa 1.87x / 0.85x, Magic 1.87x / 0.85x, Resto 1.74x / 0.79x, Sova 1.74x / 0.79x, Ultima 1.77x / 0.81x, Riva 1.62x / 0.74x, Luma 1.58x / 0.72x. Luma is the tightest because it has the most bands; its slabs end up about as thick as the air between them.
- **Validation:** `npm run build` clean, `oxlint` clean on both changed files (the only warnings in the tree are pre-existing, in `ProductTransition.jsx`). The squash was checked arithmetically for all eight products: the closed stack comes back to the product's declared height exactly in every case. **Not yet checked on screen** - these are geometric constants chosen from the arithmetic, and 2.2x and the 15px label pitch are the two numbers most likely to want a nudge once seen.

---

### 2026-08-31 — Every layer gets its real name
- **Changed:**
  - **The numbered `Layer N — TBD` stand-ins are gone.** The product owner named every band of all eight layered products, and the names are now the labels the exploded stack shows. Two of them are shared rather than per-product: the cover is **AirKnit Fabric** everywhere except Riva, and the bonded transition-sheet-plus-base band is **Quilted Foam** in every product of both brands. The base name is fixed inside the `base` factory in each layer file rather than passed in at the call site, because there is no per-product variation to express and a parameter would only invite one.
  - **The names corroborate Luma's numbering rather than merely fitting it.** The owner gave layers 5 and 7 the same name, **Guard Flex**, and put **Hybrid Pocket Springs** between them at layer 6. That is exactly the pair of coil insulator pads either side of the spring unit — so the renumbering of 2026-08-27, which moved Luma's spring unit from Layer 5 to Layer 6 when the blue comfort band was added at 3, is confirmed from an independent direction. Every other product's band count matched its named list on the first pass, with nothing left over and nothing missing.
  - **Duro's cover name is retired.** "Super Plush LuxeKnit Fabric" was the one signed-off material name this project carried, and it is replaced by AirKnit Fabric on explicit instruction — put to the product owner first, because `docs/PRODUCT_CATALOG.md`'s precedence rule makes a Confirmed entry win over anything that appears to supersede it until the owner says otherwise. Duro's layer 3 also moves from `Ortho Bond` to `OrthoBond`; layer 2 keeps the `AeroFlex` it already had. The cover's layer id changes from `luxeknit` to `cover`, which nothing referenced.
  - **Riva has layer 1 only.** Its cover is **Bio Weave** — the one cover in either brand that is not AirKnit Fabric — and its layers 2-6 keep their numbered stand-ins and their `nameTbd` flag until the rest arrive. The `foam` factory now takes a name that may be null and falls back to the stand-in, so a partially named stack needs no second code path.
  - **One house format governs the spellings.** A closed compound takes an internal capital — **AirKnit**, **AeroFlex**, **OrthoBond** — and names written as separate words stay separate. The owner first wrote Airknit and Aeroflex flat and OrthoBond camelCase; asked which was canonical, they approved the camelCase form and it is applied to all three. `Memorest` is the one remaining name where the same question could be asked and has not been — it is left exactly as the owner wrote it.
  - **Nothing about how a band is built changed.** No `type`, `surface`, `thicknessRatio` or `color` was touched: a name is copy, and how a band looks is established by the reference cutaway renders. `description` stays filler for every band; the owner gave names, not copy.
- **Reason:** Product owner, 2026-08-31, supplying the layer names for both brands. Four ambiguities were put to the owner rather than guessed: which of three spellings of OrthoBond is canonical (`OrthoBond`), whether AirKnit Fabric really retires Duro's signed-off LuxeKnit cover (yes), whether "Bio Weave" was Riva's layer 1 (yes), and whether the flat spellings should follow the approved camelCase format (yes — applied in the same change).
- **Files/areas:** `app/src/data/layers/foamicoLayers.js`, `app/src/data/layers/vedasleepLayers.js`, `app/src/lib/foamSurfaces.js` (comment only), `docs/PRODUCT_CATALOG.md`.
- **Impact:** Label copy in the exploded layer view for all eight layered products; no geometry, material, colour, thickness or interaction change. `name` is the only field the UI reads from a layer def, and it is read in one place (`MattressViewer.jsx`).
- **Open, flagged to the product owner:** Sova's layer 5 is named **Plush Core Latex** and Ultima's layer 6 **Zero G Latex**, but both are still rendered as foam with the surfaces their cutaway renders show; Ultima's layer 5 is named **Cloud Sense Foam** while its render and role describe a rebonded chip core. Asked which was right, the owner answered "Plush Core Latex" — read here as confirming the name, so **no `type`, `surface` or render was changed on the strength of a name**. Zero G Latex and Cloud Sense Foam were not separately addressed and stay open. Riva's layers 2-6, Duro's other sizes, and the **Natural** compositions for Sova, Ultima and Riva are all to follow (owner, 2026-08-31).
- **Validation:** `npm run build` clean. Grepped for stale references to `luxeknit`, `LuxeKnit`, `Ortho Bond`, `Airknit` and `Aeroflex` across `src/`, `docs/` and `guidelines/`; the only remaining hits are historical changelog entries, which record what was true when written and are left alone.

---

### 2026-08-27 — Stage grey, a visible Riva, quilting that reads, and two loops put on the clock
- **Changed:**
  - **VedaSleep's two product-facing pages sit on Stage Grey `#D3D3D3`.** The card grid and the product pages, plus the route-loading holding screen — every VedaSleep route that reaches that screen is one of those two pages, and leaving it on Paper would flash cream before every grey page, which is the flash it exists to prevent. Paper `#F7F5F0` stays as the ground everywhere else: the page ground and the VedaSleep panel on the brand selector. The instruction said those two pages only; the holding screen is included because it *is* those two pages, painted a beat early. One effect worth noting either way: a near-white ticking now separates from its ground, which on cream it barely did.
  - **Natural is marked by its colour alone.** The filled highlight — warm gold wash, tinted border, heavier type, on the menu row and on the pill both — is gone, and the row is set in Kiwi Green. The hairline above it stays: it is what makes "pinned to the end" read as an order rather than as an odd sort, not part of the highlight. Only FOAMICO products offer Natural, so this never puts Kiwi Green on a VedaSleep screen.
  - **Riva is visible.** Its border is a charcoal non-woven that photographs at `#191919`–`#202020` — Key Black, to within a couple of levels of the stage it stands on — so nine inches of mattress rendered as a one-inch white pancake, the quilt panel being the only part of it with any tone against the ground. Nothing was wrong with the model, the texture or the light, and no lighting change fixes it: a matte fabric at 0.95 roughness has no highlight to give and its diffuse response cannot exceed its own albedo. The FOAMICO viewer now carries a soft pool of Egg White at 6% behind the model, lifting the centre of the stage to about `#262626` and falling to Key Black at the edges. Always on, never animated — it is the stage, not a state.
  - **Quilt cells are in the normal map, not only in the mesh.** They were reconstructed into the cap tessellation alone: about a third of an inch of relief across six feet of mattress, two or three pixels on screen, and nothing at all in the shading, so every product's ticking was lit as though printed on a flat sheet. The same puff field is now folded into the height map the weave comes from, referenced against the slope it actually contributes so one setting suits a lattice as coarse as Riva's and one as busy as Resto's. Cells dome and channels sink at any camera distance.
  - **Quilt loft is stated in inches and capped against the quilted panel**, instead of taken as a fraction of the mattress's total thickness. Read the old way, one ticking puffed 0.24″ on a 6″ grade and 0.40″ on a 10″ one — one product, two different quilts, and the difference showed on the silhouette every time a grade changed. At 0.30″ it lands within a couple of hundredths of what a 7″ mattress rendered before, so no product's cap moves noticeably from this alone. The dead-flat ring where the panel meets its binding narrows from a tenth of the cap's radius (three and a half inches) to 0.07, with `capRings` raised to carry the steeper ramp.
  - **Two animation loops move off the frame and onto the clock.** The camera's damper took a flat 8% of the remaining distance per frame — twice as fast on a 120Hz panel as on a 60Hz one, and visibly slowing and speeding up again through any dropped frame — and the idle auto-orbit stepped a fixed angle per frame. Both now advance on the frame's own `dt`, with the damper's time constant set so a move has settled by `MOTION.camera`, which reproduces the old rate exactly at 60fps.
  - **The layer labels stop forcing layout every frame.** Each visible band read `getBoundingClientRect` and `offsetWidth` after the band before it had written its styles — eight forced synchronous layouts per frame through the whole explode and every grade change, and none at all in the camera path. The stage box is measured in the resize handler and label widths are cached against the text they were measured for, re-measured when the stage resizes because the phone breakpoints change the label's type size.
  - **The layer stack no longer slams shut.** Its separation was a decelerating curve on a linear clock, which is right opening and backwards closing: the bands drifted at first and then covered the last 27% of their travel in the final 70ms, arriving at full speed. The curve is flat at both ends now, so exploding and collapsing are mirror images in time as well as in path, and it stays a pure function of the clock so reversing part-way through cannot make a position jump.
  - **The normal-map hot loop is about three times faster.** `normalFromHeight` is on the startup path of every product - each procedural foam surface goes through it and so does each quilt - and it was spending most of its time on two avoidable things: a `%` per sample when the offsets are only ever ±1 and the wrap can only matter on the four borders, and `Math.hypot`, which is specified to avoid intermediate overflow and pays several times a plain `sqrt` for protection a gradient and a 1 will never need. Rows now carry precomputed neighbour offsets and the column wrap is a comparison. The arithmetic that reaches the byte is untouched, and the output was checked byte for byte against the previous implementation across all five FOAMICO bump maps at both ends of the strength range: zero differing bytes. `quiltMaps` as a whole now costs less than it did *before* the puff compositing was added to it.
  - **The cap's edge dip is sized from the displacer's real amplitude**, carried on the function, instead of from `displace(0, 0)` — the relief at the exact centre of the mattress, which lands wherever that product's pattern happens to put it and is as likely to be a channel as a crown.
- **Reason:** Product owner, 2026-08-27: apply `#D3D3D3` to the VedaSleep product and card pages; remove Natural's highlight and colour its type Kiwi Green; the quilting is not accurately displayed; Riva is not visible on its product page; the transitions are not smooth.
- **Files/areas:** `app/src/data/brandThemes.js`, `app/src/pages/CatalogPage.jsx`, `app/src/App.jsx`, `app/src/index.css`, `app/src/components/MattressViewer.jsx`, `app/src/lib/quiltSurface.js`, `app/src/lib/mattressGeometry.js`, `app/src/lib/foamSurfaces.js`, `app/src/data/foamicoProducts.js`, `guidelines/DESIGN_GUIDELINES.md`, `docs/INTERACTIONS.md`, `docs/MATERIALS_AND_TEXTURES.md`, `docs/CAMERA_AND_LIGHTING.md`, `docs/PERFORMANCE.md`, `docs/3D_RENDER_GUIDELINES.md`.
- **Impact:** Every mattress in both lines gets a quilt that shades — this is the widest-reaching change here, and it is meant to be. Every FOAMICO product now stands on a faintly lifted stage; pale products are unaffected by it and Riva goes from unreadable to a full nine inches. VedaSleep's two product pages change ground colour. No product data, no layer composition, no dimension and no variant list is touched.
- **Validation:** Grade compositions re-checked across all five FOAMICO products after the change, unchanged. Reference photography re-sampled to establish Riva's border tone against the stage tone before deciding the fix. Quilt output inspected at close range on the highest-contrast ticking (Resto) and the lowest (Ultima, standard deviation 6/255) for embossing and banding artefacts. `normalFromHeight`'s rewrite is checked byte for byte against the old one (5 products x 2 strengths, 0 differing bytes) and timed either side. The label fix is verified by inspection rather than by benchmark - the loop makes no layout read at all now, where before it made one per visible band per frame - because the only browser available here runs on software GL at about a frame a second, which is far too few frames for a per-frame cost to show up in a measurement. The same limitation applies to the two clock fixes: their correctness is arithmetic (the new rates reproduce the old ones exactly at 60fps) and what they fix is behaviour at refresh rates this environment cannot produce. **All three want a look on real hardware.** `oxlint` and `vite build` clean.

---

### 2026-08-27 — Luma gains the blue comfort band its top grade always had
- **Changed:**
  - **A band was missing.** `LUMA LUXURY.png` shows eight bands; this repository's Luma stack had seven and no blue comfort foam at all, so the product's own top grade could never have rendered correctly. The blue band is added at layer 3 and **Luxury alone carries it** - Premium 8″ and Classic 6″ drop it, which is what the other two renders show.
  - **Every band below it renumbered.** Numbering follows position in the full stack, the same as every other product, so Luma's spring unit now reads **Layer 6** rather than Layer 5. The product owner referred to it as layer 5, which is what it was in the seven-band stack; it is the same band either way.
  - **The new band's proportion is solved, not chosen.** The rule given was that Luxury's spring unit matches Premium's. With the other seven ratios summing to 0.95, that fixes the blue band at 0.2375 - which makes it exactly 2.00″ and leaves every other band in Luxury the identical thickness it has in Premium. Luxury is therefore exactly Premium plus this one layer, which is what the renders show. Classic's spring unit lands 33% shorter than Premium's, the visible difference that was asked for.
  - **Its colour is not a new value.** Sampled off the render, Luma's blue lands within a few points of Resto's blue sampled the same way from the same set, so it takes `#29B6DC`, the albedo already established for that foam, rather than introducing a second near-identical blue.
- **Reason:** Product owner, 2026-08-27. The instruction named "layer 3 in luxury", which was ambiguous until the render set showed Luxury carrying a band the code had no equivalent for; the product owner confirmed it is the blue one.
- **Files/areas:** `app/src/data/layers/foamicoLayers.js`, `app/src/data/foamicoProducts.js`, `docs/PRODUCT_CATALOG.md`.
- **Impact:** Luma Luxury renders eight bands where it rendered seven, and Classic and Premium render the same seven as before minus the blue - so all three grades change. No other product is affected.
- **Validation:** Proportions solved and checked to sum exactly to each declared height, with Luxury's and Premium's spring units equal to within a thousandth of an inch and every other band identical between them. Verified in a browser: Luxury shows layers 1-7 + base, Premium and Classic show 1, 2, 4, 5, 6, 7 + base with the gap at 3. Console, `oxlint` and `vite build` clean.

---

### 2026-08-27 — Sova, Ultima and Riva build their grades from different stacks
- **Changed:**
  - **Sova** - Premium 6.5″ drops layer 3; Classic 6″ and Classic 5″ drop layers 3 and 4. The product owner gave one rule for "sova classic" and Sova presents two Classic heights, so both take it.
  - **Ultima** - Premium 6″ drops layer 3; Classic 6″ and Classic 5″ drop layers 3 and 4. Classic 6″ and Premium 6″ are now the same height with different builds, which is intended.
  - **Riva** - R2000 8″ drops layer 2; R1000 6″ drops layers 2 and 3. Riva thins from the top of the comfort stack downwards, the opposite of how the others thin.
  - **`holdUpholstery` is now opt-in per product, and only Resto sets it.** Holding the cover and bonded base at their baseline thickness reads correctly across Resto's 6″-7″ spread and badly across wider ones - it would have made Riva's base 42% of an R1000. Every other product divides its grade's height in proportion to the ratios the bands already have, so their upholstery scales with the mattress exactly as it did before. This is a change in where the rule applies, not in what it does; Resto renders identically.
  - **Luma is deliberately untouched** and still shows every band at every grade - see the open question recorded against it in `docs/PRODUCT_CATALOG.md`.
  - **Natural is untouched** for all three products that offer it; its composition is still to come.
- **Reason:** Product owner, 2026-08-27, extending to the rest of the line the per-grade construction already implemented for Resto.
- **Files/areas:** `app/src/data/foamicoProducts.js`, `app/src/lib/variantLayers.js`, `docs/PRODUCT_CATALOG.md`.
- **Impact:** Sova, Ultima and Riva now render fewer bands below their top grade. No product's upholstery proportions change. Luxury/R3000 and Natural grades are unchanged everywhere, as is Resto.
- **Validation:** Band sets and thicknesses solved for every product and grade and checked to sum exactly to each declared height. Each rule cross-checked against the product owner's own renders - `SOVA CLASSIC.png` and `SOVA PREMIUM.png`, `ULTIMA CLASSIC.png`, `RIVA 2K.png` - all of which show exactly the band sets given. `oxlint` and `vite build` clean.

---

### 2026-08-27 — The per-grade cutaway render set enters the repository
- **Changed:** The product owner's 26 cutaway renders - one per product per grade - are committed at `Layers/`, at the repository root, because the set spans both brands and splitting it by brand would break the comparison that makes it useful. Names are as delivered. This supersedes the `Foamico mattresses/Natural/` folder added earlier the same day, whose three files were byte-identical duplicates of the Natural renders in the set; that folder is removed rather than left as a second copy.
- **Reason:** It is the reference art the layer stacks are read from, and it was living outside the project. Having it in the repository is also what let the badge on `SOVA NATURAL.png` be settled on evidence.
- **Files/areas:** `Layers/` (new, 26 files, ~52 MB), `Foamico mattresses/Natural/` (removed), `docs/ASSET_MANAGEMENT.md`, `docs/PRODUCT_CATALOG.md`.
- **Impact:** Source only - nothing is processed into `app/public/`, nothing is referenced by the experience, and no product data is derived from it beyond Resto, which the product owner had already given and which `RESTO CLASSIC.png` and `RESTO PREMIUM.png` independently corroborate. Adds ~52 MB to the repository permanently, which is the cost of having the art versioned with the code that reads it. Three of the files are Signature, which is no longer a product; they are kept only because they came with the set, and the rule against re-adding Signature from its catalog entry alone applies to them too.
- **Validation:** All 26 files checksum-verified against the originals after copying. The three Natural files confirmed byte-identical to the folder they replace, so nothing was lost in the swap.

---

### 2026-08-27 — Resto's grades build different stacks, and Paper warms to #F7F5F0
- **Changed:**
  - **Resto's grades are now different builds, not one build cut to three thicknesses.** Luxury 7″ is the full seven-band stack; **Premium 6.5″ drops layer 3**, and **Classic 6″ drops layers 3 and 4**. The base is present in every grade. A variant may now declare `omitLayers` - the ids it leaves out - which is subtractive on purpose: it is the direction the product is specified in, and a list of what is missing cannot accidentally drop the base the way a list of what is present could. Every other product declares nothing and gets its whole stack back by identity, so nothing else in either line changes.
  - **Bands that survive keep their own numbers.** Classic reads Layer 1, Layer 2, Layer 5, Layer 6, Base. The gap is the information - it shows which bands the grade leaves out - and it keeps one band meaning one thing across all three grades.
  - **The cover and the bonded base now hold their real thickness across Resto's grades** and the surviving foam divides the rest of the declared height. This follows the catalog's own framing that a foam split is a percentage of the foam, so a thinner Resto reads as less foam rather than as a puffier cover. **This also changes Classic and Premium where nothing is dropped**: their cover and base previously scaled down with the mattress and no longer do (Classic's base 1.71″ → 2.00″, cover 0.55″ → 0.64″). Luxury 7″ is unchanged.
  - **Choosing a grade is a transition rather than a swap.** The build on screen releases, the geometry changes at the trough where nothing is visible, and the new build settles in: in the solid view from the old thickness into the new one, and with the stack open by bands leaving and arriving in the explode's own top-down rhythm. It runs at `MOTION.normal` on the loop's existing `easeOutCubic`, sharing one clock with the explode and only ever multiplying what the explode already draws, so no new duration or easing enters the motion system and the two cannot fight. Both halves run one curve, so the gesture is its own reverse. Reduced motion applies the change without it. The pill's label cross-fades at `MOTION.fast`.
  - **Paper is `#F7F5F0`**, warmed from `#F6F8F1`. Changed at all five sites that read it so the card grid, the product pages, the page ground, the route-loading holding screen and the VedaSleep panel on the brand selector cannot drift apart - changing only the two pages would have flashed the old off-white through the holding screen on every navigation. The dormant `paper` alternative in `FoamicoCatalogPage` is left as it was; it is a parked surface, not a VedaSleep ground.
- **Reason:** Product owner: Resto's lower grades are genuinely built from fewer comfort-foam layers, and the dropdown should show that rather than only changing the thickness. The warmer ground was requested for the VedaSleep pages.
- **Files/areas:** `app/src/lib/variantLayers.js` (new), `app/src/data/foamicoProducts.js`, `app/src/components/MattressViewer.jsx`, `app/src/index.css`, `app/src/App.jsx`, `app/src/data/brands.js`, `app/src/data/brandThemes.js`, `app/src/pages/CatalogPage.jsx`, `docs/PRODUCT_CATALOG.md`, `docs/INTERACTIONS.md`, `docs/3D_RENDER_GUIDELINES.md`, `guidelines/DESIGN_GUIDELINES.md`.
- **Impact:** Resto's Classic and Premium render different stacks and slightly different proportions than before; Luxury is unchanged, as is every other product in both lines. The trough that hides the geometry swap waits for a rebuilt stack but is bounded at `MOTION.explode`, so a failed build settles anyway rather than leaving the product invisible - `ensureStack` already swallows a failed build to keep the viewer usable, and an unbounded hold would have undone that.
- **Validation:** Driven in Chrome over CDP against the dev server. Resto at all three grades in both the solid and exploded views: band sets 7 / 6 / 5 with labels 1-6+Base, 1,2,4,5,6+Base and 1,2,5,6+Base; the morph settles back to full opacity every time, including after rapid switching between grades. Sova, Luma (the coil stack), Ultima, Riva, Duro, Maxa and Magic still show every band at every grade. Narrow viewport (390×844) menu and selection; `prefers-reduced-motion` applies the change instantly. VedaSleep catalog and product pages both computed `rgb(247, 245, 240)`. Console clean; `oxlint` and `vite build` clean.

---

### 2026-08-26 — Top grade by default, Signature retired, and real foam splits
- **Changed:**
  - **Every product page opens on its top grade.** Baselines moved from the grade a product happened to present to its best one: **Luxury** for Resto, Sova, Luma, Ultima and Duro, **R3000 9″** for Riva - it has no grade called Luxury and its R ladder tops out there - and Classic for Maxa and Magic, which have no second grade. The baseline still drives `dimensions.height`, so each page renders the thickness of the grade it opens on.
  - **Signature is removed from the experience**, entirely: product data, page, route, preload entry, layer stack, grid card and its four textures. Old `/#/signature` links fall through to the brand selector. Its catalog entry is kept as the record of what was confirmed about it, marked as removed - and the two things flagged against it (the unverified construction and the "Pocket Spring + Foam" spec line against a coil layer of None) went with the product.
  - **The layer detail card is gone.** Tapping a band in the exploded stack used to open a card with the band's name, role, description and thickness; every one of those fields was a placeholder, so the card promised a specification that does not exist yet. The exploded stack and its hover labels are now the whole of the layer view. Touch keeps a tap-to-name gesture - the label lights and fades on its own, since touch has no hover to stand in for it - and the hint reads "Drag to rotate · Solid to collapse".
  - **Foam splits, from the product owner.** Each one divides only the foam bands named in it; the cover, any convoluted top band and the bonded base sit outside it and keep the proportions they already had, so the split changes how the foam divides, not how thick the upholstery is.
    - **Resto, Sova, Ultima** - layer 6 30%, layer 5 30%, layer 4 20%, layer 3 20%.
    - **Luma** - the spring unit (layer 5) 80%, layer 3 20%.
    - **Magic** - layer 3 25%, layer 4 75%, replacing the 80 / 15 / 5 solved for it earlier. The convoluted band keeps its 5% of the foam.
    - **Maxa** - layer 3 is 100% of the foam and **the rebonded support core under it is removed**; Maxa is four bands now. It no longer shares a template with anything, so its stack is written out.
    - **Duro** - Ortho Bond is 100% of the support foam with AeroFlex left as it is, which the owner confirmed leaves the stack unchanged.
  - **Luma's spring unit fills its band's footprint.** It was laid out at a fixed pitch from a fixed 3″ inset and then handed a footprint already shrunk to 94%, so the springs stopped several inches short of the edge on all four sides and read as a smaller mattress inside the mattress. The pitch still sets how many springs there are; the spacing is now solved to fill the width and length it is given, less the 2% the slabs' rounded corners take.
  - **Sofa cum Bed: the plate has room around it.** It was butted against the title above and the buttons below, so a full-width white panel read as the page's background rather than as something being shown on it. It is now centred as a flex child with real padding - an absolutely positioned box lays out against the padding box, which is why the first attempt at this changed nothing.
- **Reason:** All requested. Three ambiguities were put to the product owner before implementing rather than guessed: what to remove from the layer card (the whole card), what Riva opens on given it has no Luxury grade (R3000), and whether "Ortho Bond 100%" removed AeroFlex (it does not).
- **Files/areas:** `src/components/{MattressViewer,SofaPhotoViewer}.jsx`, `src/data/{products,foamicoProducts,brandThemes}.js`, `src/data/layers/{foamico,vedasleep}Layers.js`, `src/lib/{coilLayer,layerStack}.js`, `src/App.jsx`, `src/routePreload.js`, `src/index.css`, deleted `src/pages/SignaturePage.jsx` and `app/public/textures/signature/`, `docs/{PRODUCT_CATALOG,INTERACTIONS,3D_RENDER_GUIDELINES,ASSET_MANAGEMENT}.md`.
- **Impact:** VedaSleep is three products. Every page opens on the grade a buyer would be shown first. The layer view no longer states any placeholder specification. Luma's spring unit reads as a spring unit the size of the mattress.
- **Validation:** `oxlint` clean of new findings (28 warnings, all pre-existing); production build passes. Percentages verified numerically against the layer data rather than by eye - each named band's share of the bands in its own split printed and checked to 0.01% (all exact: Resto / Sova / Ultima 20/20/30/30, Luma 20/80, Magic 25/75, Maxa 100). Driven over CDP in headless Chrome against the built bundle: every page's opening pill checked (Duro Luxury 6″, Resto Luxury 7″, Sova Luxury 7″, Luma Luxury 10″, Ultima Luxury 6.5″, Riva R3000 9″, Maxa and Magic Classic 5″); the VedaSleep grid confirmed to be Duro / Maxa / Magic and `/#/vedasleep/signature` to land on `#/`; band counts confirmed (Maxa 4, Magic 5, Resto 7, Luma 7); a tap on an exploded band confirmed to open nothing on all four; Luma's coil grid checked at pixel level against the slab edge. All 12 routes loaded with an empty console at 1400×900 and 390×844, no horizontal overflow at 390px, and the sofa plate measured at 30px clear of the title and 26px clear of the buttons.
  - **A harness bug is worth recording:** hash-only navigation does not reload the document, so a run that switches between `#/a` and `#/b` keeps whatever bundle the tab already had. An early pass on this change appeared to show none of it working; every route now reloads past the cache.
- **Known remaining:**
  - Both grids still say "Tap a product to explore it in 3D", still not true of Sofa cum Bed. Unchanged from the previous entry.
  - Every layer in every product is still named "Layer N — TBD" with placeholder colours. Removing the card removed the placeholder *copy* from the screen; the labels remain, and the real material names are still outstanding.
  - Sova and Ultima still list Classic at two thicknesses each.
  - Resto, Sova and Ultima now share one set of split percentages, but each keeps its own combined foam share, so their stacks are not identical. If those three are meant to be the same product in different covers, that is worth saying explicitly.

---

### 2026-08-26 — Grades on the page, not the card; the Sofa cum Bed photographed
- **Changed:**
  - **Grid cards lost their subline**, on both brands. A card used to read "RESTO / Classic · 6″ Firm"; it now reads "RESTO". The grade and thickness are a choice the product page offers, not one fact to quote on the way in, and quoting one of several made the card look like the whole answer. `specLine` stays in the product data as the record of the baseline — nothing renders it — and the now-unused `spec` colour came out of the card themes.
  - **Every dropdown reads thinnest-first**, so the list scans as a size ladder (5″ → 6″ → 6.5″ → 7″) rather than by grade name. Ties hold the order the data declares, so Ultima's Classic 6″ still leads its Premium 6″. Display order is computed from `variants` and carries each entry's index with it, so the selection is still held as an index into the data — the order on screen never becomes a second, disagreeing source of which variant is chosen.
  - **The menu no longer repeats the pill.** It lists what you can switch to, so the variant already named above it is filtered out. Sova's menu was showing "Classic · 6″" directly underneath a pill saying "Classic · 6″".
  - **Natural, at 6″, added to Riva, Sova and Ultima**, pinned to the end of every menu whatever it measures and drawn in warm gold with a hairline above it — it is the premium grade of the products that offer it, not a rung on the size ladder. The gold is fixed rather than brand-themed: on VedaSleep, whose accent is already gold, a themed highlight would say nothing. The pill takes the same gold while Natural is selected.
  - **Luma's grades corrected by the product owner** to Classic 6″, Premium 8″, Luxury 10″. The previous entry had raised Luma from 6″ to 8″ on this table's authority; the table was wrong, and Classic is 6″ — the figure the code originally carried. There is no 5″ Luma.
  - **Riva is presented by its R grades plus Natural** — R1000 6″, R2000 8″, R3000 9″, Natural 6″ — and by nothing else. The Classic / Premium / Luxury rows the catalog also lists for it are no longer offered, and neither is its second Natural at 8″. **Ultima's Natural moved 7″ → 6″**: Natural is one 6″ grade wherever it appears.
  - **Sofa cum Bed is photographed rather than rendered.** Its page now shows three studio plates — **Front** (opened flat as a bed), **Side** (part-folded from the end), **Sitting** (folded upright as a seat) — switched by the same control row the mattress pages use for camera presets, in the same chrome, with the same entrance. New `src/components/SofaPhotoViewer.jsx`. The 3D model and viewer built for this product in the previous round are **kept but no longer routed**; nothing imports them, so they cost nothing in the bundle, and the photography could be superseded.
- **Reason:** All requested by the product owner. The Luma, Ultima and Riva figures supersede `docs/PRODUCT_CATALOG.md`, which has been updated to record the override in the same change, per `CLAUDE.md`.
- **Files/areas:** `src/components/{MattressViewer,ProductCard,SofaPhotoViewer,SofaViewer}.jsx`, `src/pages/{FoamicoCatalogPage,foamico/SofaCumBedPage}.jsx`, `src/data/{foamicoProducts,brandThemes}.js`, `src/index.css`, `app/public/products/sofa-cum-bed/{front,side,sitting}.jpg`, `docs/{PRODUCT_CATALOG,INTERACTIONS,ASSET_MANAGEMENT,3D_RENDER_GUIDELINES}.md`.
- **Impact:** The grids state what a product is called; the page states what it can be. Every menu reads the same way on both brands, and Natural is legible as a different kind of choice from the thickness above it. The Sofa cum Bed route dropped from pulling the 557 kB chunk that carried three.js to a 2.5 kB page — three.js now lands in `ProductPage`, which every mattress route needed anyway.
- **Validation:** `oxlint` clean of new findings (28 warnings, all pre-existing); production build passes. Driven over CDP against the built bundle in headless Chrome: every one of the nine menus dumped and checked — order, contents, and that the pill's own variant is absent from each (Sova `[Classic 5″, Premium 6.5″, Luxury 7″, Natural 6″]`, Riva `[R2000 8″, R3000 9″, Natural 6″]`, Luma `[Premium 8″, Luxury 10″]`, Signature's six ordered 4″ before 5″, Maxa's single variant still a label with no menu). Selecting Natural on Riva turned the pill gold (measured `rgb(217,178,90)`) and put R1000 back into the menu. Both grids confirmed to render the product name alone. Sofa page checked in all three positions at 1400×900 and 390×844: the right plate opaque and the other two at zero each time. All 13 routes loaded with an empty console and no horizontal overflow at 390px.
  - The first pass at the sofa plates used the uncropped frames and the product sat small in a field of white; they are now cropped to the subject's own bounding box (shadow included) before being fitted to 3:2.
- **Known remaining:**
  - Both grids still say "Tap a product to explore it in 3D", which is not true of Sofa cum Bed now that it is photographed. Flagged rather than reworded — it is the grid's headline copy and not in scope here.
  - **Signature's spec line still records "Pocket Spring + Foam" while the catalog records its coil layer as None.** Unchanged from the previous entry, and now invisible on screen — the card no longer renders it — but still wrong in the data.
  - Maxa's 6″ → 5″ and Signature's 8″ → 5″ corrections still want confirmation. Luma's has now been resolved by the owner; Riva's was superseded by presenting only its R grades.
  - Sova and Ultima each list a Classic at two thicknesses, so their menus show "Classic" twice at different sizes. That is what the catalog confirms, so it stands — but if a grade is meant to be one thickness, these are the rows to look at.

---

### 2026-08-26 — A variant selector on every product page

- **Changed:**
  - **Product pages now carry a variant control** under the product name: a pill stating which variant is on screen, opening a list of the rest. Choosing one **rebuilds the mattress at that variant's thickness** - the solid box, its sculpted quilt cap, the stitch along the seam, the woven badge's position on the base band, the ground shadow and the exploded stack all follow. The layer detail card's Thickness figure follows too, so the bands it describes still add up to the mattress on screen.
  - A variant change **rebuilds only the geometry that encodes thickness**, not the scene. Everything else - renderer, lights, environment, textures, materials, camera - is thickness-independent, so re-running the whole scene would drop the WebGL context, re-fetch every texture and throw away where the user had orbited to, to arrive at the same pixels. The old bands are disposed only once the new ones exist, so changing thickness with the stack open swaps one stack for another rather than blinking through an empty stage.
  - **Variant data is numbers, not labels.** Each product declares `variants: [{ variant, height }]`, baseline first, `height` in inches. The baseline is the single source of the product's rendered thickness: `dimensions.height` and the grid card's spec line are both derived from it, so the card and the viewer cannot drift apart.
  - **Four products were rendering a thickness the catalog does not confirm**, and now render their baseline variant instead: **Luma 6″ → 8″** (Classic is 8″; there is no 6″ Luma), **Riva 8″ → 6″** (the collection presents Riva at its 1000 grade, and R1000 is 6″ - 8″ is R2000), **Maxa 6″ → 5″** and **Signature 8″ → 5″** (Signature's confirmed variants are 4″ and 5″ in three grades; 8″ appears nowhere). Their grid spec lines changed with them.
  - Two products were labelling their **category** as their variant: Maxa's "Comfort" and Magic's "Memory Foam" are the ranges those products sit in; the confirmed variant of each is Classic. Both cards now say Classic.
  - `buildLayerStack`'s `dispose()` now removes its meshes from the group as well as freeing them. It only ever ran at unmount, where the whole group went with it; a stack rebuilt under a live scene would otherwise leave the previous variant's bands hanging in the frame.
- **Reason:** Requested. The four thickness corrections are `guidelines/DEVELOPMENT_GUIDELINES.md` §10 applied - a control that offers the catalog's variants cannot open on a thickness the catalog contradicts - and are flagged rather than quietly made; see Known remaining.
- **Files/areas:** `src/components/MattressViewer.jsx`, `src/lib/layerStack.js`, `src/data/{products,foamicoProducts}.js`, `src/index.css`, `docs/{PRODUCT_CATALOG,INTERACTIONS}.md`.
- **Impact:** Every product whose catalog entry lists more than one variant is explorable at each of them; the four single-variant products state their variant as a label rather than offering a choice that isn't one. Nine of the site's products gained the control; Sofa cum Bed has no thickness to drive and gained nothing.
- **Validation:** `oxlint` clean of new findings; production build passes. Verified against the live scene graph over CDP rather than by eye, because SwiftShader will not composite the canvas into a CLI screenshot: Resto's box height tracked 6.082 → 7.095 → 6.588 → 6.082 across four variant changes with `group.children` (20), mesh count (29) and triangle count (37452) unchanged throughout - i.e. no orphaned geometry - and the shadow held at -H/2-1.5 each time. Riva's badge stayed at exactly two decals and rode the base band from y -1.20 at 6″ to -1.65 at 9″ and back. Duro rebuilt its stack while exploded, 5.145 → 6.174, counts unchanged, and its layer card restated the same band as 1.0″ then 1.2″ - 20% of the mattress either way. All 13 routes load with an empty console. Layout checked at 390, 360 and 1280: no horizontal overflow, the menu inside the viewport at each, rows 39px tall.
  - **A real bug was caught this way and fixed:** the menu was painted *and hit-tested* underneath the canvas, so every option was unclickable for an actual user - programmatic clicks in the first test pass had hidden it by bypassing hit-testing. The entrance animation puts a transform on the control's wrapper, which traps any z-index inside it in a stacking context of its own; the tier now sits on the wrapper. Re-verified with dispatched mouse events at real screen coordinates, on phone and desktop.
  - The pill's caret is drawn in CSS rather than set as `▾`: neither brand face carries that glyph, and the fallback rendered as a dot, which reads as punctuation rather than as "this opens".
- **Known remaining:**
  - The four thickness corrections above **want the product owner's confirmation**. They follow the catalog, which outranks the code, but each replaces a figure someone may have put there deliberately.
  - **Signature's spec line still reads "Pocket Spring + Foam" while the catalog records its coil layer as None** (and flags Signature's whole construction as not independently verified). Only the thickness in that line was corrected; the coil claim is a separate question and was left alone rather than resolved by inference.
  - Per-layer thicknesses scale with the chosen variant, which is right while every layer is a ratio. **Magic's proportions are absolute** - cover 0.35″ and bonded base 0.60″ are real figures solved against 5″ - and Magic has one variant, so nothing scales today. A product with absolute layer thicknesses and more than one variant would misstate them, and would need its own per-variant figures.
  - Sova, Ultima and Riva list variants that differ only in grade at the same thickness (Riva's Classic 6″, Luxury 6″ and Natural 6″ all render identically). The control changes thickness; it does not yet express what else a grade changes, because the catalog does not record it.

---

### 2026-08-26 — One bonded base band, Magic's real proportions, and a broken triangle wave
- **Changed:**
  - **Brand selector:** both panels now reserve the same height for their mark, so the two "View collection" cues sit on the same line. They were 20px apart because each panel centres its own column and the VedaSleep lockup is shorter than FOAMICO's.
  - **The transition sheet and the base are one layer, in all nine products.** The pierced orange foam is bonded to the top of the fabric-wrapped base, so the stack must never separate them. They now build as a single band — one label, one card, one hover, one explode step — with the sheet keeping its own sculpted relief on top so it still reads as foam pasted onto the base. Every product's sheet:base split is exactly its previous transition:base ratio, so the band looks the same as before; what changed is that it no longer comes apart. Ultima, Riva, Resto and Sova drop from 8 bands to 7, Luma 8 to 7, Duro 5 to 4, the VedaSleep three 6 to 5.
  - **Magic** is 5″ (the 6″ in code was never confirmed; the catalog had it as TBD) and no longer uses the shared `vedaFiveBand` template. Cover 0.35″ and bonded base 0.60″ take real thicknesses first, then the 4.05″ of foam splits 80% / 15% / 5% — support core 3.24″, comfort foam 0.61″, convoluted top 0.20″. Maxa and Signature keep the template until their own numbers arrive.
  - **Fixed a displacement bug that had been wrong on half of every sculpted sheet.** The pyramid surface's triangle wave was `Math.abs(((v / cell) % 1) * 2 - 1)`. Slabs are centred on the origin, and JavaScript's `%` takes the sign of the dividend, so across the whole x<0 / z<0 half the wave was inverted and peaked at **2.98 instead of 1** — cutting 1.175″ into Ultima's 0.875″ sheet. It was invisible while the sheet was a band with nothing beneath it. Bonding the base underneath surfaced it immediately, as a fan of grey base cloth showing through the orange.
  - `createLayerMaterials` now reports `displaceDepth`, so a caller stacking something under a sculpted slab can see how much of it the relief has already eaten. The bonded band uses it to weld its two slabs together with real clearance rather than a guessed offset.
- **Reason:** All requested. The triangle-wave bug was found while diagnosing the grey bleed-through and is a genuine pre-existing defect, not a consequence of the merge.
- **Files/areas:** `src/pages/BrandSelectPage.jsx`, `src/lib/{layerStack,layerMaterials}.js`, `src/data/layers/{foamico,vedasleep}Layers.js`, `src/data/products.js`.
- **Impact:** Every mattress's stack has one fewer band and the base reads as a single bonded unit. The pyramid relief is now uniform across the whole sheet on every product. Magic's card reads 5″ and its stack is dominated by the support core as specified.
- **Validation:** `oxlint` clean of new findings; production build passes. Diagnosed the bleed-through by tinting the body magenta to confirm which surface was surfacing, then reproduced the wave bug numerically against JS `%` semantics (`fmod`) before fixing it. Screenshotted exploded stacks for Magic, Duro, Luma, Ultima and Riva at several angles; cue alignment measured at rows 356–363 on both brand panels. Magic's ratios verified to sum to 1 and to hit 80.0% / 15.0% / 5.0% of the 4.05″ foam portion exactly.
- **Known remaining:** Maxa and Signature still carry the old template proportions, awaiting their numbers.

---

### 2026-08-26 — Riva's badge, the base band's sides, and Sofa cum Bed in 3D
- **Changed:**
  - **Riva border badge.** The supplied crop was mirrored, so the woven badge read as a reversed "AVIR / SLEEP"; and the badge was baked into the photograph the wall repeats about six times around the perimeter, so it appeared six times, down the long sides included. The crop is un-mirrored, the badge painted out of it with clean fabric from the badge-free half (verified: zero pixels left above the detection threshold), and the badge now ships as `side-badge.png` and is placed as a decal on the head and foot faces only. Declared per product via `sideBadge`, never by slug; Riva is the only product in either line that carries one.
  - **Exploded base band.** Its wall wore `productSideMap` — the whole mattress's gusset photograph, spanning cover, piping and border, squeezed onto a band under an inch tall — which is what made the bottom slab read charcoal on top and cream down its sides. All three faces now wear the base cloth. `productSideMap` is gone from the layer pipeline entirely.
  - **VedaSleep grid page.** Removed the text "VEDASLEEP" wordmark under the lotus; the lockup contains the name, so the page was saying it twice. The mark takes the size the wordmark was carrying (40px → 62px).
  - **Sofa cum Bed is now 3D**, replacing the photo page from the previous entry. New `src/lib/sofaModel.js` (three hinged panels, seat position) and `src/components/SofaViewer.jsx`. No Layers control — the product has no layers.
  - Supporting moves so the second viewer duplicates nothing: `BRAND_THEMES` out of `MattressViewer` into `src/data/brandThemes.js`, and the `.mv-*` chrome out of its inline `<style>` into `src/index.css` (an inline style outranks a media query, which is why that sizing was in a stylesheet to begin with).
  - `buildEuroTopGeometry` now reports its base-wall extent in `userData`, so a caller placing something on that band reads the proportions off the geometry instead of re-deriving them.
- **Reason:** All four requested. Items 1–3 were raised as "found, not fixed" in the previous entry.
- **Files/areas:** `src/components/{MattressViewer,SofaViewer,ProductCard}.jsx`, `src/lib/{sofaModel,mattressGeometry,layerMaterials,layerStack}.js`, `src/data/{brandThemes,foamicoProducts}.js`, `src/pages/{CatalogPage,foamico/SofaCumBedPage}.jsx`, `src/index.css`, `app/public/textures/foamico/riva/`, `app/public/textures/sofa-cum-bed/`.
- **Impact:** Riva carries its badge twice, the right way round. The base band reads as one slab in both views. Six products on the FOAMICO grid, all of them explorable in 3D — so the grids' "explore it in 3D" is now true of every card. three.js moved into a chunk shared by both viewers; `ProductPage` dropped from 589 kB to 27 kB and the sofa route does not pull `MattressViewer`.
- **Validation:** `oxlint` clean of new findings; production build passes. Screenshotted in headless Chrome (SwiftShader WebGL): brand selector, both grids, Riva corner and front (badge once, centred, correct orientation), Ultima exploded (base band consistent), Duro, and Sofa cum Bed in corner and side. Sofa fabric tone measured against the source photograph — backrest 123 vs 116, seat front 123 vs 116. Fabric tile verified seamless with no grain loss at the seam (22.3 edge vs 21.2 centre) and large-scale drift cut from 5.6 to 2.0.
- **Known remaining:** the Sofa cum Bed's flat bed position is modelled but not reachable — no control is wired to the fold, per instruction not to add one yet. The bolster seen in the bed-position photographs is not modelled. The six gallery plates from the previous entry were deleted as orphans when the page became 3D; the source photography is untouched.

---

### 2026-08-26 — Brand marks, base-layer colour, and a photographed product
- **Changed:**
  - Brand selector: removed the FOAMICO positioning tagline, and removed both the text "VEDASLEEP" wordmark and the VedaSleep tagline. Both marks are lockups that already carry the brand name.
  - Brought the supplied VedaSleep logo into the repo: cropped and white-keyed to a 1156×449 transparent PNG (was a 417×171 crop), and sized it to match FOAMICO's *width* so the two panels carry equal weight.
  - Both grid pages: subtitle reduced to "Tap a product to explore it in 3D".
  - Product pages: removed the `Corner` and `Detail` view pills. The corner framing itself is unchanged — it is still the mount default and where `Solid` returns — and is now named by `CORNER_VIEW` instead of being a magic number in five places. `Detail` was the only preset that moved the camera dolly, so the resize guard protecting it also went.
  - Product pages: removed the variant/thickness and warranty sublines from the header, on every product in both brands. The header is now mark + product name. The dead `specsPending` branch and its two theme tokens went with it; `specLine` stays in the data because the grid cards still use it.
  - **Base-layer colour, all nine products:** re-derived each bottom band's colour from the median of its own `bottom.png`. Eight of nine declared a light tan/cream base against an actual charcoal underside (e.g. Ultima `#A89E8B` → `#464846`, Duro `#DCD3BE` → `#646263`); Signature was the only near-match and still had the wrong hue (`#B9AE96` → `#B79692`).
  - **Solid underside:** added a bounce source aimed straight up, and gave the base cloth the woven normal map it never had. The underside was rendering at RGB ~30 against a ~26 page — invisible — and is now at ~71, matching the `#474847` the cloth photographs at.
  - **New product:** Sofa cum Bed under FOAMICO. A tri-fold folding unit with no layers, presented through its own photography on a new `SofaCumBedPage` rather than through the 3D viewer, which can only build a single slab.
- **Reason:** Requested. The base-layer and underside defects were found while investigating the reported "bottom colour mismatch" and quantified against the reference photography.
- **Files/areas:** `src/pages/BrandSelectPage.jsx`, `src/pages/{Catalog,FoamicoCatalog}Page.jsx`, `src/components/{MattressViewer,ProductCard}.jsx`, `src/data/foamicoProducts.js`, `src/data/layers/{foamico,vedasleep}Layers.js`, `src/pages/foamico/SofaCumBedPage.jsx`, `src/App.jsx`, `src/routePreload.js`, `app/public/brand/vedasleep-logo.png`, `app/public/products/sofa-cum-bed/`, and the docs below.
- **Impact:** Both brand panels read as equal choices. Undersides read as cloth in both the solid and exploded views. FOAMICO gains a sixth product whose page pulls no three.js at all (4.5 kB chunk).
- **Validation:** `oxlint` clean of new findings; production build passes. Screenshotted in headless Chrome (SwiftShader WebGL) at 1400×900–1100: brand selector, both grids, Ultima corner / underside / exploded, and the Sofa cum Bed page. Underside tone measured against the reference at RGB [70.9, 71.5, 69.3] vs [71, 72, 71]. Corner and exploded views confirmed visually unchanged by the new light.
- **Known remaining:** two defects found but left alone as out of scope — see the notes below.

#### Found, not fixed at the time (all four resolved in the entry above)
- `app/public/textures/foamico/riva/side.png` is **mirrored**: the woven badge reads "AVIЯ / ꓒƎƎ⅂ꓢ" instead of "RIVA / SLEEP". Flipped somewhere in the original crop.
- In the **exploded** view the bottom band's side wall wears `productSideMap` — the *whole mattress's* gusset photograph, squeezed onto a ~0.9″ band — so the base still reads charcoal on top and cream on its sides. Offered and explicitly not selected in this round.
- The VedaSleep grid page still sets a text "VEDASLEEP" wordmark under the lotus lockup, so the brand name now appears twice there. The same removal as the brand selector would fix it; only the selector was in scope.
- Both grids say "explore it in 3D", which is not true of Sofa cum Bed.

---

### 2026-08-26 — Documentation baseline
- **Changed:** Created project documentation and AI-agent guidelines.
- **Reason:** Establish a maintainable source of truth for the 3D mattress experience.
- **Files/areas:** `CLAUDE.md`, `README.md`, `guidelines/`, `docs/`.
- **Impact:** Future development can follow explicit product, design, 3D, interaction, asset and performance rules.
- **Validation:** Documentation created from the available project/deployed-page context; unspecified implementation details remain marked for verification.
